import { Wrapper } from "@/components/Wrapper";

// this app is so far doing nothing with RSC
// wrapper is a component which handles all the drag n drop of the report json as well at the chart rendering.
export default function Home() {
    return <Wrapper />;
}
